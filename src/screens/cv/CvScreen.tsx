import { StyleSheet, Text, View } from 'react-native'
import React, { memo } from 'react'

const CvScreen = memo(() => {
    return (
      <View>
        <Text>CvScreen</Text>
      </View>
    )
  })

export default CvScreen

CvScreen.displayName = "CvScreen"

const styles = StyleSheet.create({})